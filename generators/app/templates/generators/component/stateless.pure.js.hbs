import React from 'react';
import PropTypes from 'prop-types';
import pure from 'recompose/pure';

const {{ properCase name }} = props => {
  return (
    <div />
  )
};

{{ properCase name }}.propTypes = {};


export default pure({{ properCase name }})
